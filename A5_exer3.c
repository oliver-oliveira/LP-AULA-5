#include <stdio.h>
#include <math.h>

int main () {
  float n1,n2, potencia;
  
    printf("X: ");
    scanf("%f", &n1);
    
    printf("Y: ");
    scanf("%f", &n2);
    
    potencia = pow(X^Y);
    printf("Potencia de %.2f = %.2f", x, n2, potencia);

    return 0;
}